apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana
  namespace: monitoring
spec:
  rules:
  - host: grafana.froystein.jp
    http:
      paths:
      - pathType: Prefix
        path: /test
        backend:
          service:
            name: grafana
            port:
              name: http
